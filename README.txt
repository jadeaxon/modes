Deployment:

Simply have this present in ~/Dropbox/Projects/Modes.

Create this link so that it autostarts on boot after everything else:
"C:\ProgramData\Microsoft\Windows\Start Menu\Programs\StartUp\Z Master.ahk.lnk"

Copy the appropriate Symbols_<resolution> folder to a folder named Symbols.

